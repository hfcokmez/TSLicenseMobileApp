import { useEffect, useState } from "react";

export const useFetch = url => {
    const[data, setData] = useState([])

    useEffect(() => {
        effect
        return () => {
            cleanup
        }
    }, [url])



    return state;
};